# ═══════════════════════════════════════════════════════════════
# AIGC 内容审核工具 · 依赖清单
# Python >= 3.8
# 更新日期：2025-01
# ═══════════════════════════════════════════════════════════════

# ── 数值计算 ──────────────────────────────────────────────────
numpy>=1.23.0,<2.0.0

# ── 可视化 ────────────────────────────────────────────────────
matplotlib>=3.6.0

# ── 信号处理（find_peaks，visualizer 模块使用）─────────────────
scipy>=1.9.0

# ── 语义向量模型（semantic_checker 模块使用）──────────────────
sentence-transformers>=2.2.0

# ── 向量检索（semantic_checker 内部相似度计算）────────────────
# 注意：faiss-cpu 在 Windows 需通过 pip 安装，Linux/Mac 同名包
faiss-cpu>=1.7.3

# ── HTTP 请求（fact_checker 模块联网核查使用）─────────────────
requests>=2.28.0

# ── 配置文件解析（标准库 configparser 已内置，无需单独安装）──
# configparser 已内置于 Python 3.2+，无需列出

# ── 类型注解兼容（Python 3.8 无 ParamSpec 等新特性兜底）───────
typing-extensions>=4.0.0